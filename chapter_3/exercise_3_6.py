""" 
다음과 같이 값을 반환하지 않는 함수를 하나 정의하고, 함수를 실행하여 반환된 값을 변수에 저장하라.
그 변수를 print() 함수로 화면에 출력해서 어떤 값이 저장되어 있는지 확인해 보라.
"""


def no_return():
    """화면에 메시지를 출력하지만, 값을 반환하지는 않는다."""
    print('이 함수에는 반환값이 없습니다.')


result = no_return()


print(result)  # return None
